# <コンポーネント名>

> このテンプレートの使い方: `docs/specs/<component-slug>.md` として複製して使う(1コンポーネント
> 1ファイル)。`<component-slug>` は `docs/architecture.md` の責務表にあるコンポーネント名を
> kebab-case にしたもの(例: `GitLab Adapter` → `gitlab-adapter.md`)。
> このセクション(引用ブロック)は複製後に削除する。
>
> 想定読者は **M4以降にこの仕様を実装の根拠として読むClaude Code自身**。実装済みコードから
> 逆算して書くのではなく、「呼び出し側がこの文書だけを読んで実装/利用できるか」を基準に書く。
> 迷ったら具体的な型・シグネチャ・エラー名を書く側に倒す(抽象的な説明だけで済ませない)。

- 実装場所: `src/gitlab_ai_platform/<module>/`
- 対応Issue: #xx
- 関連ADR: [ADR-XXXX](../adr/XXXX-xxx.md)(設計判断がある場合。なければ「なし」と書く)
- ステータス: 未着手 / 実装中 / 実装済み

## 責務

<このコンポーネントが何をするか。1〜3文。`docs/architecture.md` の責務表の記述と矛盾しないこと
(表を要約するのではなく、実装レベルまで具体化する)。>

## 前提と非対象

- 前提: <このコンポーネントが依存する外部条件・前提となる他コンポーネントとの関係
  (例: 「呼び出し前にState Storeへの接続が確立していること」)。>
- 非対象: <やらないこと。`docs/architecture.md` の「境界」列と対応させる。実装者が
  「なぜここに書かれていないのか」で迷わないよう、意図的に対象外にしたものを明記する。>

## 公開インターフェース

<呼び出し側から見える関数・クラス・メソッドのシグネチャ。型ヒント込みで、実際にimportできる
コードに近い形で書く(擬似コードで済ませない)。Protocol/ABCを使う場合はその型も含める。>

```python
# 例
def method_name(arg: ArgType) -> ReturnType:
    """1行で役割を説明する。"""
```

## 入出力スキーマ

<やりとりするデータの型定義(dataclass/TypedDict/JSON Schema等)。フィールドごとに
「必須か」「どこから来るか」が分かるようにする。>

## エラー時の振る舞い

<どんな例外/エラー型を送出するか、呼び出し側はそれをどう扱うべきか(リトライする/しない、
握りつぶしてよいか)。「実装が失敗しうる箇所とその結果」を列挙する形で書く。>

## テスト方針

<何を保証するテストがあるか・あるべきか。`tests/` 配下の対応パス
([ADR-0001](../adr/0001-repository-structure.md) の通り `src/` をミラーする)。
外部依存(GitLab API等)をどうモック/フィクスチャ化するかも書く。>

## 関連ドキュメント

- [architecture.md](../architecture.md) の該当行
- 関連ADR(あれば)
- ソースコードパス
